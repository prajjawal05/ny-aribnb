import styled from "styled-components";

const StyledBurst = styled.div`
    .highlighted {
        cursor: pointer;
    }

    body .sunId {
        overflow: visible;
    }
`;

export default StyledBurst;